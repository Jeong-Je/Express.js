<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel = "stylesheet" href="stylesheet/style.css">
    <title><%= post.dataValues.title %></title>
</head>
<body>
    <span><%= post.dataValues.formattedDate %></span>
    <span><%= username %></span><br>
    제목<h1><%= post.dataValues.title %></h1>
    내용<h2><%= post.dataValues.content %></h2>
    댓글
    <% if(comment) { %>
        <% for (let i = comment.length -1; i >= 0; i--) { %>
            <h3><%= comment[i].User.dataValues.username %> <%= comment[i].dataValues.content %> <%= comment[i].dataValues.formattedDate %></h3>
        <% } %>
    <% } %>

    <form action="/comment_upload/<%= post.dataValues.id %>" method="post">
        <textarea class="post_upload_content" name="content" cols="30" rows="5" required></textarea><br>
        <button type="submit">댓글 달기</button>
    </form>
    <button type="button" onclick="location.href='/'">글목록</button>
</body>
</html>